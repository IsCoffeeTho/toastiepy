from setuptools import find_packages, setup

setup(
    name='toastiepy',
    packages=find_packages(include=['toastiepy']),
    version='0.1.0',
    description='',
    author='IsCoffeeTho',
    install_requires=[],
    setup_requires=[]
)
